# copy this into your loop to enable generation of blocks
# left click to block tile and right click to unblock tile

# pos = pygame.mouse.get_pos()  # gets the current mouse coords
# if event.type == pygame.MOUSEBUTTONDOWN:
#     for i in range(rows):
#         for j in range(cols):
#             rect = gameBoard.boardArray[i][j]
#             if rect.is_inside_me(pos):
#
#                 if event.button == 1:
#                     gameBoard.boardArray[i][j].block()
#                 if event.button == 3:
#                     gameBoard.boardArray[i][j].unblock()
